import { ABCLayout } from './components/ABCLayout'

// Компонент — просто функция, которая возвращает JSX (разметку)
function App() {
  return <ABCLayout />
}

// export default — «главный» экспорт файла, один на модуль
export default App
